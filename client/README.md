# Email Client

This is vite app written in React using TypeScript.

## Run

The client can be started via:

```shell
yarn dev
```

## Compile UIKit SASS

```shell
yarn compile:theme
```
